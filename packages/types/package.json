{
  "name": "@ai-email-campaign/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types and interfaces for AI Email Campaign Writer",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "zod": "^3.22.0"
  }
}
